import React from "react";
import VerifyEmail from "@/components/pages/auth/verify";

const page = () => {
	return (
		<div className="flex justify-center items-center w-full h-screen">
			<VerifyEmail />
		</div>
	);
};

export default page;