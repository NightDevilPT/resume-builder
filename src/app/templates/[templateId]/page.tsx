import React from "react";

const TemplatePage = () => {
	return (
		<div>
			{/* In that page we will showing the template details */}
			{/* we will showing the template details like template name, template description, template thumbnail, template category, template layout, template sections, template styling, template metadata, template created by, template created at, template updated at, template is published, template is active, template usage count, template rating, template tags */}
			{/* This template page not editable, only user can see the template details with the create resume button with this template */}
		</div>
	);
};

export default TemplatePage;
