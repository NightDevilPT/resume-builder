import React from "react";

const TemplatesPage = () => {
	return (
		<div>
			{/* we will showing all templates in this page so user can see all templates and select one of them to create a new resume */}
		</div>
	);
};

export default TemplatesPage;
